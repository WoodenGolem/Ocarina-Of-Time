#include "game.h"


int main()
{
	Game game;

	/*
	glm::vec3 normal = { 0,1,0 };
	glm::vec3 vector = { 0,10,0 };
	glm::vec3 projec = vector - (glm::dot(vector, normal)) * normal;

	std::cout << projec.x << " " << projec.y << " " << projec.z << std::endl;
	getchar();
	*/

	return 0;
}